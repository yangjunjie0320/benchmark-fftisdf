name: fftisdf
channels:
  - defaults
  - conda-forge
dependencies:
  - python==3.9
  - defaults::numpy
  - defaults::scipy
  - compilers
  - cotengra
  - opt_einsum
  - pip
  - pip:
    - pyscf
    - line_profiler
    - ase
